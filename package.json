{
  "name": "moore-park-beach-inn",
  "version": "1.0.0",
  "description": "Moore Park Beach Inn website",
  "author": "Shaun Nielsen",
  "license": "MIT",
  "source": "src/index.html",
  "scripts": {
    "prestart": "rimraf dev-srv",
    "start": "parcel --dist-dir dev-srv",
    "prebuild": "rimraf docs",
    "build": "parcel build --public-url ./ --dist-dir docs",
    "postbuild": "copyfiles -u 1 src/robots.txt src/sitemap.xml src/CNAME docs"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.12.0",
    "copyfiles": "^2.4.1",
    "parcel": "^2.12.0",
    "rimraf": "^6.0.1"
  }
}
